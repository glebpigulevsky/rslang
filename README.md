# Rslang application 
### Installation

Install the dependencies and devDependencies.

```sh
$ npm i
```

Then run dev-server: 
```sh
$ npm run start
```

To run build in dist:
```sh
$ npm run dev
```

Make code in ```/src``` folder
